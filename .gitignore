# Credentials — never commit
.env
deploy/.env
*.key
*.pem

# Local multi-module workspace — must never be committed
go.work
go.work.sum

# Build artifacts
/bintrail
/bintrail-mcp
/bintrail-console
/dist/

# Test coverage
cover.out
/tmp/bintrail-cover/

# Claude Code
.claude/
CLAUDE.md

# Python
__pycache__/
*.pyc

# macOS
.DS_Store
docker-compose.override.yml

# Local scratch space for design notes — never published (#tidy-root).
drafts/

# tokenade editor artifacts
*.tokenade-original
